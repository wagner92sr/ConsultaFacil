<h1 class="page-header">Minhas consultas</h1>

<table class="table table-hover"> 
    <thead> 
       <tr> 
          <th>Id</th> 
          <th>Data</th> 
          <th>Médico</th>
          <th>Hospital</th>
          <th>Status</th>
          <th></th>
          <th></th>
        </tr> 
    </thead>
     <tbody>
        <tr> 
        <% @appointments.each do |app| %>
           <td><%= app.id %></td>                        
           <td><%= app.date %></td> 
           <td><%= app.doctor.name %></td>
           <td><%= app.hospital.name %></td>
           <td><%= app.status %></td>    
           <td>           
           <%= link_to edit_site_profile_appointment_path(app.id), class:"btn btn-primary btn-sm" do%>
              <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
           <% end %>
           </td>
           <td>
           <%= link_to site_appointment_detail_path(app.id), class:"btn btn-primary btn-sm" do%>
              <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
           <% end %>             
           </td>         
        <% end %>
        </tr>       
      </tbody> 
</table>



